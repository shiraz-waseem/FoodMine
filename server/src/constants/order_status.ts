export enum OrderStatus {
  NEW = "NEW",
  PAYED = "PAYED",
  SHIPPED = "SHIPPED",
  CANCELED = "CANCELED",
  REFUNDED = "REFUNDED",
}

// By enum you have a structure and you can use it as type so that field will be limited to item that this enum offers
// these all are strings
